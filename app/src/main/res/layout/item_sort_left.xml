<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable name="item" type="com.zhuzichu.orange.sort.viewmodel.ItemLeftViewModel"/>
    </data>
    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
                 xmlns:app="http://schemas.android.com/apk/res-auto"
                 xmlns:tools="http://schemas.android.com/tools"
                 android:orientation="vertical"
                 android:layout_width="match_parent"
                 android:foreground="?attr/selectableItemBackground"
                 app:onClickCommand="@{item.clickItem}"
                 android:background="@{item.selected?@color/colorBackground:@color/white}"
                 android:layout_height="wrap_content">
        <androidx.appcompat.widget.AppCompatTextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingLeft="10dp"
                android:paddingRight="10dp"
                android:paddingTop="15dp"
                android:paddingBottom="15dp"
                android:layout_gravity="center"
                android:text="@{item.sortBean.main_name}"
                android:textColor='@{item.selected?@color/colorPrimary:@color/colorPrimaryText}'
                android:textSize="15sp"
                tools:text="男装"/>
    </FrameLayout>
</layout>
