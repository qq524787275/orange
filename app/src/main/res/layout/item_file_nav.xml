<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable name="item" type="com.zhuzichu.orange.ui.file.viewmodel.ItemNavViewModel"/>
    </data>
    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
                  xmlns:tools="http://schemas.android.com/tools"
                  xmlns:app="http://schemas.android.com/apk/res-auto"
                  android:layout_width="wrap_content"
                  android:layout_height="match_parent"
                  android:layout_gravity="center"
                  android:background="?android:attr/selectableItemBackground"
                  app:onClickCommand="@{item.onClickItem}"
                  android:clickable="true"
                  android:focusable="true"
                  android:gravity="center">

        <androidx.appcompat.widget.AppCompatTextView
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:drawableEnd="@drawable/ic_arrow_right_file_picker"
                android:drawableRight="@drawable/ic_arrow_right_file_picker"
                android:drawablePadding="16dp"
                android:ellipsize="middle"
                android:gravity="center"
                android:maxEms="12"
                android:paddingStart="16dp"
                android:paddingLeft="16dp"
                android:singleLine="true"
                android:textAllCaps="false"
                android:textColor="#FF48282c"
                android:textSize="16sp"
                android:textStyle="bold"
                android:text="@{item.fileName}"
                tools:text="SD 存储卡SD 存储卡SD 存储卡SD 存储卡"/>
    </LinearLayout>
</layout>