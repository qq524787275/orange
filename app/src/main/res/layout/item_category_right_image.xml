<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <import type="com.zhuzichu.orange.R"/>
        <variable name="item" type="com.zhuzichu.orange.category.viewmodel.ItemImageViewModel"/>
    </data>
    <com.zhuzichu.mvvm.view.layout.SquareRelativeLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:layout_width="match_parent"
            android:foreground="?attr/selectableItemBackground"
            android:background="@{item.color.contentBackground}"
            app:isScale="@{true}"
            app:onClickCommand="@{item.clickItem}"
            android:layout_height="wrap_content">
        <LinearLayout
                android:layout_width="match_parent"
                android:orientation="vertical"
                android:layout_centerInParent="true"
                android:gravity="center"
                android:layout_height="wrap_content">
            <androidx.appcompat.widget.AppCompatImageView
                    android:layout_width="50dp"
                    android:layout_height="50dp"
                    android:scaleType="centerCrop"
                    app:url="@{item.image}"
                    app:enableWhiteToAlpha="@{true}"
                    android:layout_marginTop="5dp"
                    app:placeholderRes="@{R.mipmap.ic_silent_plate_mix}"
                    tools:src="@drawable/ic_launcher_background"/>
            <androidx.appcompat.widget.AppCompatTextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    tools:text="男装"
                    android:textSize="13sp"
                    android:text="@{item.name}"
                    android:layout_marginTop="8dp"
                    app:textColor="@{item.color.textColorSecond}"/>
        </LinearLayout>
    </com.zhuzichu.mvvm.view.layout.SquareRelativeLayout>
</layout>